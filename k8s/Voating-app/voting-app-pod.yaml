apiVersion: v1
kind: Pod
metadata:
  name: voting-app-pod
  labels:
    name: voating-app-pod
    app: demo-voting-app
spec:
  containers:
    - name: voting-app
      image: kodekloud/examplevotingapp_vote:v1
      ports:
        - containerPort: 80